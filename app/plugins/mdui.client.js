// MDUI client-side plugin
// Registers mdui custom elements for Vue compatibility
import 'mdui/mdui.css'

export default defineNuxtPlugin(() => {
  // MDUI CSS is imported above; JS will be loaded client-side
})
